import { PhasePending } from '@/components/layout/PhasePending';

export function PaperReviewPage() {
  return <PhasePending title="Paper review" phase={5} description="Per-question review and mistake classification." />;
}
