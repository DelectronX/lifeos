import { PhasePending } from '@/components/layout/PhasePending';

export function GoalsPage() {
  return <PhasePending title="Goals" phase={2} description="Goals, milestones and the tasks beneath them." />;
}
